services:
  redis:
    image: redis:alpine
    networks:
      - oort-test

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      POSTGRES_PASSWORD: secret
    networks:
      - oort-test

networks:
  oort-test:
    name: oort-test
